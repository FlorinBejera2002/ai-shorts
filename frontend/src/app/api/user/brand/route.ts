import { NextResponse } from 'next/server'

export function GET() {
  return NextResponse.json({ message: 'Brand API — Phase 6' })
}

export function PUT() {
  return NextResponse.json({ message: 'Brand API — Phase 6' })
}
