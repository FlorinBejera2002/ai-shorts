# OpenNext uses this selector while previewing through Wrangler. Frontend
# configuration contains public transport origins only. Keep credentials in
# the separate Go backend environment, never in Next.js or Worker files.
NEXTJS_ENV=development
